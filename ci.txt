departamento (		profesor (		grado (
    id,			id_profesor,		   id,
    nombre		id_departamento,	nombre,	
);			);			);

persona (			CREATE TABLE asignatura (
    id,				    id,
    nif,			    nombre,
    nombre,			   creditos,
    apellido1,			tipo ENUM(3),
    apellido2,			    curso,
    ciudad,			   cuatrimestre,
    direccion,			   id_profesor,
    telefono,			id_grado,
    fecha_nacimiento,		    );
    sexo,				
    tipo			
);				
 
CREATE TABLE curso_escolar (		alumno_se_matricula_asignatura (
    id,					  id_alumno,
    anyo_inicio,			id_asignatura,
    anyo_fin				id_curso_escolar,
);						);
